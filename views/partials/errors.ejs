<!-- NOT USED, possibly use in a future project -->
<div class="errors">
  <% if (locals.errors) {%>
  <ul>
    <% errors.forEach(function(error) { %>
    <li><%= error.msg %></li>
    <% }); %>
  </ul>
  <% } %>
</div>